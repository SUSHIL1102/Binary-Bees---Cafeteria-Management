# MongoDB: must include database name (e.g. /cafeteria before ?)
# Atlas: mongodb+srv://user:pass@cluster.xxxxx.mongodb.net/cafeteria?retryWrites=true&w=majority
# Local: mongodb://localhost:27017/cafeteria
DATABASE_URL="mongodb://localhost:27017/cafeteria"
JWT_SECRET="your-super-secret-key-change-in-production"
PORT=3001
NODE_ENV=development

# W3 SSO (OIDC) - from w3 SSO Provisioner / Team BB - Blu-Reserve System
# Must match exactly what is registered in w3 SSO Provisioner
W3_CLIENT_ID="YzQ0MGVlMWEtN2RlOC00"
W3_CLIENT_SECRET="ZGI1NTQwOGYtNzAyZC00"
W3_AUTH_URL="https://preprod.login.w3.ibm.com/oidc/endpoint/default/authorize"
W3_TOKEN_URL="https://preprod.login.w3.ibm.com/oidc/endpoint/default/token"
W3_REDIRECT_URI="http://localhost:3001/api/auth/w3/callback"

# Frontend URL (where to redirect after w3 login)
CLIENT_URL="http://localhost:5173"
